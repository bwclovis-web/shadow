# Archived utility scripts

One-off maintenance scripts kept for reference. Prefer `lib/note-materials/` and `npm run notes:seed-materials` for note cleanup (see `scripts/NOTE-MATERIALS.md`).

| Script | Purpose |
|--------|---------|
| `count-unattached-notes.js` | Count `Note` rows not linked to any perfume via `PerfumeNoteRelation` |

Moved from `scripts/` in Phase 4 architecture cleanup (June 2026).
