/**
 * App-wide constants
 */

export const TRADER_FEEDBACK_RATING_OPTIONS = [1, 2, 3, 4, 5] as const

/** Max length for trader about text on user/trader profile (nullable field). */
export const PROFILE_LENGTH = 500
